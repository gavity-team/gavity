# Changelog

## 未发布

### 🚀 功能

- 添加管理后台
  - 初始管理员创建方法：在前端注册账号，然后设置 app 容器环境变量 `DO_SET_ADMIN_EMAIL` 为该账号的邮箱，重启 app 容器即可
- 支持通过 `LOG_LEVEL` 环境变量设置日志级别
- 优化用户相关的 UI 组件外观

## 🏠 其他更改

- ⚠ 大多数配置从环境变量移至管理后台
- ⚠ 新增 Redis 服务依赖

### 🩹 修复

- 修复记录模式开关的可见性

## 0.2.0 (2026-07-31)

### 🚀 功能

- 不要求每次登录验证邮箱
- 拆分会议 ID 与入会码
- 服务启动时自动验证环境变量，避免以错误配置启动

### 🩹 修复

- 修复 logo 显示

### 💅 重构

- ⚠ 重命名环境变量 `BETTER_AUTH_SECRET` 为 `SECRET`
- ⚠ 合并环境变量 `SMTP_HOST` `SMTP_PORT` `SMTP_USER` `SMTP_PASS` 为 `SMTP_URL`

## 0.1.0 (2026-07-30)

🚀 Let's go!
